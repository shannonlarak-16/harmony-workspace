# harmony demo (JavaScript)

A minimal Node.js demo that loads a harmony encoding and renders a
conversation. Currently uses **CommonJS** because the upstream
`@openai/harmony` package ships a CommonJS NAPI build.

## Run

```bash
npm install
npm start
```
